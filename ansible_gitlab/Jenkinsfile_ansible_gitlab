pipeline {
	agent any
	stages {
		stage('Git Clone') {
			steps {
				git ([url: "https://github.com/samodev/Projet_finale.git", branch: 'master' ])
			}
		}

		stage('ansible-playbook') {
			steps {
				sh "ansible-playbook ansible_gitlab/installGitlab.yml -u stage"
			}
		}
	}
}